import 'package:flutter/material.dart';

const Color azulEscuro = Color(0xFF001B59); // Azul escuro
const Color azulPrincipal = Color(0xFF06153A); // Azul escuro
const Color backgroundColor = Color(0xFF1B326A); // Azul escuro
const Color azulMedio= Color(0xFF223164); // Azul médio/escuro
const Color azulClaro = Color(0xFF9EB7D5); // Azul acinzentado claro

const Color marromEscuro = Color(0xFF65471C); // Marrom/Ocre
const Color marromQueimado = Color(0xFF946B2D); // Marrom/Ocre
const Color marromClaro = Color(0xFFCFA964);
const Color marromClarinho = Color(0xFFE3C2B2); // Pêssego/bege claro

const Color begeClaro = Color(0xFFECE1D0);   // Bege muito claro/creme
const Color cinzaEscuro = Color(0xFF5D5D5D); // Cinza
const Color cinzaClaro = Color(0xFFAEAEAE); // Cinza
const Color pretoPadrao = Color(0xFF000000); // Preto
const Color brancoPadrao = Color(0xFFFFFFFF); // Branco puro

// Cores das Casas de Hogwarts
const Color grifinoriaColor = Color(0xFFDC2626); // Vermelho Grifinória
const Color lufaColorr = Color(0xFFEEB804); // Dourado Grifinória
const Color sonserinaColor = Color(0xFF16A34A); // Verde Sonserina
const Color corvinalColor = Color(0xFF2563E7); // Azul Corvinal

// === Aliases temporários
const Color lightPrimaryBrandColor   = marromClaro;   // botão principal
const Color lightSecondaryBrandColor = azulEscuro;    // botão secundário
const Color lightTertiaryBrandColor  = azulMedio;     // botão terciário